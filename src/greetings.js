export const greetings = (name) 
    => console.log(`hello ${name}, how are you doing ?`);
export const goodBye = (name, nextMeetingDate) 
    => console.log(`good bye my friend ${name}, let's meet on ${nextMeetingDate}`);